#include<stdio.h>
int main()
{
  int n=12;
  if(n>0)
        printf("the number is positive\n");
  else if (n<0)
    printf("the number is negative");
  else
    printf("the number is zero");

    return 0;
}
